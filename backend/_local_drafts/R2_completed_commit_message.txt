feat: implement US28 (Admin Tool Categories) + US33 (Moderation Reports)

US28 — Admin Manages Tool Categories  [NEW]
  - Category model (tool_categories table) replaces hardcoded ToolCategory enum
  - Migration: tools.category converted from ENUM to VARCHAR(100),
    tool_categories table created, seeded with 5 original categories
  - CategoryService: add (duplicate guard), remove (active-listing guard),
    validate (used by ToolService on create/update)
  - GET /api/v1/categories (member, read-only)
  - POST /api/v1/categories (admin, 201, records admin ID+timestamp)
  - DELETE /api/v1/categories/{id} (admin, 409 if ACTIVE tools use it)
  - Tool model, schema, service, route updated to use dynamic string categories
  - 8 acceptance tests covering all 4 ICEBOX scenarios

US33 — Admin Generates Community Moderation Reports  [NEW]
  - ModerationReportService aggregates: audit log, listing reports,
    reservations (totals by state, summary only, no PII in summary)
  - GET /api/v1/admin/reports/moderation — JSON report with summary + records
  - GET /api/v1/admin/reports/moderation/export — CSV export with headers
  - Date range filter on both endpoints
  - 5 acceptance tests covering all 4 ICEBOX scenarios + date filter

Infrastructure
  - New Alembic migration: e4f6a8b2c3d1 (tool_categories table,
    tools.category ENUM->VARCHAR, seed categories)
  - test_tools.py + factories.py: ToolCategory enum references replaced
    with string literals
  - conftest.py: seed default categories after schema creation
  - All 198 tests passing (3 skipped, 3 xfailed, pre-existing)
